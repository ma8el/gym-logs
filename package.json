{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt --hostname localhost dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@nuxtjs/supabase": "^0.3.1",
    "nuxt": "^3.3.2",
    "typescript": "^5.0.3"
  },
  "dependencies": {
    "@mdi/font": "^7.2.96",
    "sass": "^1.60.0",
    "vuetify": "^3.1.4"
  }
}
